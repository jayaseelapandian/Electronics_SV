module tb;
  adder_if aif();

  adder dut (
    .a(aif.a),
    .b(aif.b),
    .sum(aif.sum)
  );

  initial begin
    aif.a = 5;
    aif.b = 7;
    #1;
    $display("a = %0d, b = %0d, sum = %0d", aif.a, aif.b, aif.sum);
  end
endmodule
